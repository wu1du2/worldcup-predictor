# low_score_basket_v0

状态：候选策略，当前已提升为 `main_strategy`。

策略说明：

- 覆盖世界杯常见低比分：`0-0`、`0-1`、`1-0`、`1-1`。
- 每个比分固定 `1` 注。
- 不根据单场赛前分析主动加码，不临时追高赔率。

设计理由：

- 正确比分市场方差很高，固定低比分篮子可以降低策略自由度，方便审计和回测。
- 该策略适合作为 AI 推荐系统的 baseline；后续增强策略必须先证明 ROI 优于此 baseline。
